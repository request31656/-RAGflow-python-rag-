# 这个 compose 文件留作占位。
# 后端能力（ES/MySQL/MinIO/Redis/ragflow 主体）全交给 ragflow 官方 compose：
#   cd ragflow-main/ragflow-main/docker && docker compose -p ragflow up -d
# ragflow 起来后访问 http://localhost:9380 注册账号、建 API key 填到 .env。
# Langfuse（可选 observability）也可在此处按需追加。
services: {}
